import 'package:flutter/material.dart';

class SaleAddScreen extends StatelessWidget {
  const SaleAddScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Vendas'),
      ),
    );
  }
}